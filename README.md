# fix-logitech-mouse-save-settings
Logi Options+ on Windows sometimes does not load properly, resulting in mouse settings not being applied. To be ran if settings are not loading correctly, this script closes the problematic processes and relaunches Logi Options+ to reenable proper mouse settings.
